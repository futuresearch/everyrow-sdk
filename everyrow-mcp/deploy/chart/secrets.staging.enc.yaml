# Staging secrets — encrypt with: sops -e secrets.staging.enc.yaml
# Requires GCP KMS access to projects/varuna-400921/locations/global/keyRings/sops/cryptoKeys/sops-key
#
# To populate: create a plaintext YAML with the staging Supabase anon key, then encrypt:
#   cat > secrets.staging.dec.yaml <<EOF
#   secrets:
#     data:
#       SUPABASE_ANON_KEY: "sb_publishable_..."
#   EOF
#   sops -e secrets.staging.dec.yaml > secrets.staging.enc.yaml
#   rm secrets.staging.dec.yaml
secrets:
    data:
        SUPABASE_ANON_KEY: PLACEHOLDER_ENCRYPT_WITH_SOPS
